import ReelsStudioClient from './ReelsStudioClient';

export const metadata = {
  title: 'Reels Studio - AFOLUKUTV OS',
};

export default function ReelsStudioPage() {
  return <ReelsStudioClient />;
}
